specVersion: "v1beta"
serviceId: "online_voting_app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "online_voting_app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
